/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true,  // Enable App Directory (Next.js 13+)
    },
  };
  
  export default nextConfig;
  